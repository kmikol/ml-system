global:
  scrape_interval: 15s
  evaluation_interval: 15s

# No scrape jobs here — Alloy handles collection and remote-writes metrics in.
# Prometheus runs with --web.enable-remote-write-receiver (set in docker-compose).

rule_files:
  - "alerts.yml"
