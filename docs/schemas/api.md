# API Schemas

These models define serving request/response contracts used by FastAPI endpoints.

Used primarily by:

- Serving endpoint payload validation (`/predict`, `/health`)
- Client-facing API contracts

--8<-- "docs/schemas/generated/api-fields.md"
