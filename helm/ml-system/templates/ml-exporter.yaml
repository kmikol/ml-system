{{- if .Values.mlExporter.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-exporter
  namespace: {{ .Values.global.namespace }}
  labels:
    app: ml-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ml-exporter
  template:
    metadata:
      labels:
        app: ml-exporter
    spec:
      containers:
        - name: ml-exporter
          image: {{ .Values.mlExporter.image }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.mlExporter.port }}
          env:
            {{- range $key, $val := .Values.mlExporter.env }}
            - name: {{ $key }}
              value: {{ $val | quote }}
            {{- end }}
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: ml-system-secrets
                  key: minio-access-key
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: ml-system-secrets
                  key: minio-secret-key
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.mlExporter.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 12
          resources:
            {{- toYaml .Values.mlExporter.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: ml-exporter
  namespace: {{ .Values.global.namespace }}
spec:
  type: {{ .Values.mlExporter.service.type | default "ClusterIP" }}
  ports:
    - port: {{ .Values.mlExporter.port }}
      targetPort: {{ .Values.mlExporter.port }}
  selector:
    app: ml-exporter
{{- end }}
