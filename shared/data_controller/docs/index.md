# Data Controller

This page is owned by the Data Controller module and is included into the central docs site.

The Data Controller is a facade over operational data storage (Postgres, object storage, and lakeFS helpers).
It exposes role-specific controllers for serving, drift monitoring, sampling, annotation, and dataset/version operations.

## Public API (auto-generated from docstrings)

::: shared.data_controller
