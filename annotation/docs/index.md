# Annotation

The Annotation job resolves labels for candidate predictions selected by the sampling stage.

It loads a UUID-to-label oracle from files and writes labels back through the Data Controller.

## API (auto-generated)

::: annotation.main
