name: Deploy Docs

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - "**/docs/**"
      - "**/*.py"  # rebuild when source docstrings change

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install docs dependencies
        run: pip install -r docs/docs-requirements.txt

      - name: Generate schema docs tables
        run: python3 docs/scripts/generate_schema_tables_docs.py

      - name: Deploy to GitHub Pages
        run: mkdocs gh-deploy -f docs/mkdocs.yml --force
