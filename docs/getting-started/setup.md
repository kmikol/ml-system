# Setup

This section covers the minimum setup needed to run the ML system locally.

## Prerequisites

- macOS or Linux
- Docker Desktop running
- Conda
- kubectl
- k3d
- helm
- make

## Environment

```bash
conda activate ml-system
```

## First-Time Notes

The runtime dependency install command may vary by your local workflow.

```bash
# TODO: confirm and add the canonical dependency install command for local runtime scripts
```

If your cluster uses non-default host ports, document them here before first run.

```bash
# TODO: add alternate host/port mapping guidance
```
